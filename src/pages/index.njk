---
title: ""
layout: "layouts/home.njk"
permalink: "/"
---

<!-- Intro -->
<section id="intro" class="wrapper style2">
  <div class="title">O que fazemos</div>
  <div class="container">
    <!-- Services -->
    <section id="services">
      <header class="style1">
        <h2>Nossos serviços, de forma simples e descomplicada.</h2>
        <!-- Image -->
        <a href="#" class="image featured">
          <img src="{{ site.path.prefix }}{{ site.path.assets }}/images/home-services-banner.webp" alt="Pessoas reunidas ao fundo e tela de computador em primeiro plano com os dizeres marketing digital." />
        </a>
        <p>Ajudamos a sua marca a ser encontrada por quem procura pelo que ela oferece.</p>
        <p>
          <strong>Organizamos sua presença</strong> no Google.<br />
          Rodamos anúncios com <strong>método e critério</strong>.<br />
          Ajustamos tudo com base no que <strong>realmente funciona</strong>.<br />
        </p>
      </header>
      <div class="feature-list">
        <div class="row">
        {% for item in services %}
          {% include "partials/panels/feature.njk" %}
        {% endfor %}
        </div>
      </div>
      <ul class="actions special">
        <li>
          <a href="{{ site.links.whatsapp }}" class="button style1 large">
            <i class="fa-brands fa-whatsapp"></i> Contato
          </a>
        </li>
        {# <li>
          <a href="#" class="button style2 large">Saiba mais</a>
        </li> #}
      </ul>
    </section>
  </div>
</section>

<!-- Method -->
<section id="method" class="wrapper style3">
  <div class="title">Como fazemos</div>
  <div class="container">
    <header class="style1">
      <h2>Resultado não vem de sorte. Vem de método.</h2>
      <p>Nosso trabalho é tornar o marketing <strong>útil para o negócio</strong>, não apenas bonito.</p>
    </header>
    <div class="row">
      {% for article in method %}
      <div class="col-6 col-12-medium">
        <section class="highlight">
          <div class="image featured"><img src="{{ site.path.prefix }}{{ site.path.assets }}{{ article.img }}" alt="{{ article.alt }}"></div>
          <h3>{{ article.header }}</h3>
          {% for paragraph in article.text %}
          <p>{{ paragraph }}</p>            
          {% endfor %}
        </section>
      </div>
      {% endfor %}
    </div>
    {# <ul class="actions">
      <li><a href="{{ site.links.whatsapp }}" class="button style3 large">Conheça</a></li>
    </ul> #}
  </div>
</section>

<!-- Highlights -->
<section id="gallery" class="wrapper style1">
  <div class="title">Casos de sucesso</div>
  <div class="container">
    <div class="row aln-center">
      {% for case in gallery %}
      <div class="col-4 col-12-medium">
        <section class="case">
          <div class="review">
            <h3>{{ case.header }}</h3>
            <div class="review-card">
              <div class="review-card-header">
                <div class="review-card-avatar">
                  <img src="{{ site.path.prefix }}{{ site.path.assets }}{{ case.reviewerImg }}" alt="{{ case.reviewerAlt }}">
                  <span class="review-card-reviewer">
                    {{ case.reviewerName }}
                  </span>
                </div>
                <div class="review-card-rating">
                  <span class="review-card-rating-outer">
                    <span class="review-card-rating-inner"></span>
                  </span>
                </div>
              </div>
              <div class="review-card-quote">
                <div class="review-card-quote-decor">
                  “
                </div>
                <div class="review-card-quote-text">
                  {% for paragraph in case.reviewText %}
                  <p>{{ paragraph }}</p>
                  {% endfor %}
                </div>
              </div>
            </div>
          </div>
          <div class="case-description">
            {% for paragraph in case.text %}
            <p>{{ paragraph }}</p>
            {% endfor %}
          </div>
          <ul class="actions">
            <li><a href="{{ case.link }}" class="button style1">{{ case.btnText }}</a></li>
          </ul>
        </section>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<section id="segments" class="wrapper style3">
  <div class="title">Segmentos atendidos</div>
  <div class="container">
    <header class="style1">
      <h2>Onde nossa experiência faz mais diferença.</h2>
      <p>
        Somos especialistas em atender mercados nos quais<br />
        a <strong>decisão do cliente não é imediata</strong>.
      </p>
      <p>
        Ela depende de <strong>presença consistente</strong> e de construção<br/>
        com <strong>contexto, confiança e informação certa</strong>.
      </p>
    </header>
    <div class="row">
      <div class="col-6 col-12-medium">
        <div class="image featured">
          <img src="{{ site.path.prefix }}{{ site.path.assets }}/images/segment.webp" alt="Mesa de computador, artigos e anotações de um planejamento de marketing digital em andamento." />
        </div>
      </div>
      <div class="col-6 col-12-medium segments-content">
        <h3>Áreas de destaque</h3>
        <p>A eficácia do nosso método é destacada no atendimento a profissionais liberais e estabelecimentos de serviços especializados. O nosso maior volume de projetos está nas áreas de <strong>medicina e saúde</strong>.</p>
        <p>O nosso perfil ideal de cliente é aquele que prioriza a entrega de <strong>resultados constantes, duradouros e consistentes</strong> ao invés de retorno imediato, embora não seja incomum acontecer.</p>
        <h3>Atuação internacional</h3>
        <p>Atendemos clientes no Brasil e também no exterior, especialmente nos Estados Unidos e no Canadá.</p>
        <p>A comunicação é feita em português, e a estratégia é adaptada à realidade de cada mercado, respeitando contexto local, comportamento de busca e expectativas do público.</p>
        <p>Esse modelo facilita o processo e <strong>evita ruídos</strong> comuns quando estratégia e execução não falam a mesma língua.</p>
      </div>
    </div>
  </div>
</section>